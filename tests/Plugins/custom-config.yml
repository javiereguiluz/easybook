parameters:
    book_slug: 'test_book'
    publishing.edition: 'test_edition'
    language: 'en'
    editions:
        test_edition:
             format: 'html'
             # labels: $addLabels ? ['table'] : [],
             theme: 'clean'

    publishing.list.tables: []
