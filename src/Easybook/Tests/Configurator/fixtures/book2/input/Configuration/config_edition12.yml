# book with some options set via Twig expressions (both statically and dynamically)
book:
    edition:            'Second edition'
    language:           '{{ ["es", "en", "it", "fr"][0] }}'
    generator:
        version:        '{{ "#{ 5.2 - 0.4 } beta#{ 3 - 2 }" }}'

    editions:
        edition11:
            price:      10
            pages:      250
