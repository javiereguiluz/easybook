<!-- Start page: #{{ item.slug }} -->
<div data-role="page" id="{{ item.slug }}"  class="item:{{ item.config.element }}" data-theme="d">

    {% include '_header.twig' %}
    
	<div data-role="content" class="content"> 
	    <h2>{{ item.title }}</h2>
	    
	    {{ item.content }}
    </div>
	<!-- /content -->

    {% include '_footer.twig' %}
        
</div>
<!-- /page: #{{ item.slug }} -->